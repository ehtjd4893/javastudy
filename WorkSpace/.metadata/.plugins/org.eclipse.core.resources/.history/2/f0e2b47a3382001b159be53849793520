package ex03_overloading;

public class Square extends Rectangle {
	int height;
	@Override
	void set(int w) {
		height = w;
	}

	@Override
	int getArea() {
		int area = (int)Math.pow(height, 2);
		return area >= 0 ? area : 0;
	}
	
}
