package ex03_overloading;

public class Square extends Rectangle {
	int height;
	@Override
	void set(int w) {
		super.set(w);
	}

	@Override
	int getArea() {
		if( height > 0 )
			return (int)Math.pow(height, 2);
		return 0;
	}
	
}
